<UserControl
    x:Class="ArticleTestApp.UserControls.GalleryItemUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ArticleTestApp.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="210"
    d:DesignWidth="210">

    <Grid Height="200" Width="200" Margin="10">
        <Grid.Background>
            <ImageBrush ImageSource="{x:Bind ViewModel.ImageURI, Converter={StaticResource ImageConverter}}"/>
        </Grid.Background>
        <Grid.RowDefinitions>
            <RowDefinition Height="35"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="35"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.Background>
                <AcrylicBrush TintOpacity="0.7" />
            </Grid.Background>
            <TextBlock Text="{x:Bind ViewModel.Name}" />
        </Grid>
        <Grid HorizontalAlignment="Stretch">
            <Grid.Background>
                <AcrylicBrush TintOpacity="0.7" />
            </Grid.Background>
            <TextBlock Text="{x:Bind ViewModel.Description}"  />
        </Grid>
    </Grid>
</UserControl>
